/**
 * @input A : Integer
 * 
 * @Output Integer
 */
int isPrime(int A) {
    int i,res=1;
    if(A==1) { res=0; return res;}
    else {
    for(i=2;i*i<=A;i++)
   {
      if(A%i==0) 
       { 
           res=0;
           break;
       }
       
   }
   return res;
}
 }
